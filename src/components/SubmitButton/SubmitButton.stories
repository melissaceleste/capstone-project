import ResetButton from './ResetButton.js'
import { action } from '@storybook/addon-actions'

export default {
  title: 'ResetButtonByName',
  component: ResetButton,
}

const DefaultResetButtonByName = args => <ResetButton {...args} />

export const PimaryResetButtonByName = DefaultResetButtonByName.bind({})

PimaryResetButtonByName.args = {
  resetFilter: action('onClick'),
}
